import React from 'react'
import { SearchFrameworkProps } from '../types';

function SearchFramework({ selected, setSelected }: SearchFrameworkProps) {
    return (
        <div>SeachFramework</div>
    )
}

export default SearchFramework;